<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.julyte.user.modules.allLive.AllLiveMpp">

	<resultMap id="resultMapObj"
		type="com.julyte.user.modules.allLive.AllLive"></resultMap>


	<select id="selectOneLogin" resultMap="resultMapObj">
		SELECT
		oymbSeq
		, oymbId
		,
		oymbPassword
		, oymbDesc
		, oymbName
		FROM oyMember
		WHERE 1=1
		AND
		oymbId=#{oymbId}
		AND
		oymbPassword=#{oymbPassword}



	</select>

	<insert id="insertPd">

		INSERT INTO oyProduct (
		oypdCate
		, oypdBrandCd
		, oypdName
		, oypdPrice
		, oypdVolumeWeight
		, oypdIdeal
		, oypdExpiraionDate
		, oypdHowtoUse
		, oypdManufacturerCd
		, oypdDistributorCd
		, oypdCountryCd
		, oypdIngredients
		, oypdFunctionalCosmetics
		, oypdCautions
		, oypdQaStandard
		, oypdCustomerService
		, oypdDelNy
		, regDateTime
		, modDateTime
		) values (
		#{oypdCate}
		, #{oypdBrandCd}
		, #{oypdName}
		, #{oypdPrice}
		, #{oypdVolumeWeight}
		, #{oypdIdeal}
		, #{oypdExpiraionDate}
		, #{oypdHowtoUse}
		, #{oypdManufacturerCd}
		, #{oypdDistributorCd}
		, #{oypdCountryCd}
		, #{oypdIngredients}
		, #{oypdFunctionalCosmetics}
		, #{oypdCautions}
		, #{oypdQaStandard}
		, #{oypdCustomerService}
		, 0
		, now()
		, now()
		)
		<selectKey resultType="String" keyProperty="oypdSeq"
			order="AFTER">
			SELECT last_insert_id()
		</selectKey>
	</insert>

	<insert id="insertSalePd">

		INSERT INTO oySaleProduct (
		oypdSeq
		, oyspSaleNy
		, oyspCouponNy
		, oyspFreeGiftNy
		, oyspTodayshipNy
		, oyspFreeShippingNy
		, oyspExclusiveNy
		, oyspSalePrice
	<!-- 	, oyspSalePeriodStart
		, oyspSalePeriodEnd -->
		, oyspOneBuyGetOneFreeNy
		, oyspTwoBuyGetOneFreeNy
		, oyspDelNy
		, regDateTime
		, modDateTime
		) values (
		#{oypdSeq}
		, #{oyspSaleNy}
		, #{oyspCouponNy}
		, #{oyspFreeGiftNy}
		, #{oyspTodayshipNy}
		, #{oyspFreeShippingNy}
		, #{oyspExclusiveNy}
		, #{oyspSalePrice}
	<!-- 	, #{oyspSalePeriodStart}
		, #{oyspSalePeriodEnd} -->
		, #{oyspOneBuyGetOneFreeNy}
		, #{oyspTwoBuyGetOneFreeNy}
		, 0
		, now()
		, now()
		)
		<selectKey resultType="String" keyProperty="oyspSeq"
			order="AFTER">
			SELECT last_insert_id()
		</selectKey>

	</insert>

	<select id="selectListForCache" resultMap="resultMapObj">
		SELECT
		oycdSeq
		, oycdName
		, oycgSeq
		FROM
		oyCode
		WHERE 1=1
		AND oycdDelNy = 0
		AND oycdUseNy = 1
	</select>

	<select id="selectListForCate" resultMap="resultMapObj">
		SELECT
		oyctSeq
		, oyctName
		, oyctParents
		, oyctDepth
		, oyctOrder
		FROM
		oyCate
		WHERE 1=1
		AND oyctDelNy = 0
	</select>

	<select id="selectList" resultMap="resultMapObj">
		SELECT
		oyctSeq
		, oyctName
		, oyctParents
		, oyctDepth
		, oyctOrder
		FROM
		oyCate
		WHERE 1=1
		AND oyctDelNy = 0
		AND oyctUseNy = 1
		AND oyctDepth = 3
	</select>


</mapper>
